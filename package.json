{
  "name": "sena-ambientes-front",
  "private": true,
  "type": "module",
  "description": "Librerías de front-end del módulo de asistencias y ambientes. `npm install` las descarga y `npm run vendor` copia las versiones minificadas a assets/vendor (que sí se sube al repositorio).",
  "scripts": {
    "vendor": "node scripts/vendor.mjs",
    "test": "node --test ambientes/tests/*.test.mjs"
  },
  "dependencies": {
    "@zxing/library": "^0.21.3",
    "gsap": "^3.13.0"
  }
}
